<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="VK_UI3.Views.ListenTogetherErrorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Ошибка совместного прослушивания"
    PrimaryButtonText="Закрыть"
    SecondaryButtonText="Скопировать логи"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}"
    FontFamily="{StaticResource VKSansDemiBold}">

    <Grid MinWidth="400" MaxWidth="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Произошла ошибка при подключении к серверу совместного прослушивания. Подробности ниже:"
            TextWrapping="WrapWholeWords"
            Margin="0,0,0,12"
            FontSize="14"
            Opacity="0.8" />

        <Border
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="6"
            Padding="8"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <ScrollViewer
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto"
                MaxHeight="300">
                <TextBlock
                    x:Name="LogTextBlock"
                    TextWrapping="Wrap"
                    FontFamily="Consolas"
                    FontSize="12"
                    IsTextSelectionEnabled="True" />
            </ScrollViewer>
        </Border>
    </Grid>
</ContentDialog>