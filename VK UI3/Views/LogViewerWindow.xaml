<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="VK_UI3.Views.LogViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Логи приложения"
    PrimaryButtonText="Закрыть"
    SecondaryButtonText="Скопировать логи"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}"
    FontFamily="{StaticResource VKSansDemiBold}">

    <Grid MinWidth="500" MaxWidth="800" MinHeight="300" MaxHeight="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Все логи приложения Music M. Используйте для диагностики проблем."
            TextWrapping="WrapWholeWords"
            Margin="0,0,0,8"
            FontSize="13"
            Opacity="0.8" />

        <Border
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="6"
            Padding="8"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <ScrollViewer
                x:Name="LogScrollViewer"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto">
                <TextBlock
                    x:Name="LogTextBlock"
                    TextWrapping="Wrap"
                    FontFamily="Consolas"
                    FontSize="12"
                    IsTextSelectionEnabled="True" />
            </ScrollViewer>
        </Border>

        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Spacing="8"
            Margin="0,8,0,0">
            <Button
                x:Name="ClearLogsButton"
                Content="Очистить логи"
                Click="ClearLogsButton_Click"
                FontFamily="{StaticResource VKSansDemiBold}" />
        </StackPanel>
    </Grid>
</ContentDialog>